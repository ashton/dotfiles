return {
  init_options = {
    extensionConfiguration = {
      askToStartBuild = true,
      allowBuiltInFormatter = true,
      signatureHelp = {
        enabled = true
      }
    }
  },
  settings = {
    codeLens = true,
    autoRunCodeAnalysis = true,
  }
}
